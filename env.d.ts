/// <reference types="vite/client" />

/* Строка выше — директива TypeScript, а не обычный комментарий.
 * Она подключает типы Vite: import.meta.env и импорты ресурсов (например, SVG).
 * Этот файл включён в tsconfig.app.json и tsconfig.storybook.json для проверки типов приложения и Storybook. */
