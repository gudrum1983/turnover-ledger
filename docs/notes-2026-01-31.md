Заметка на 2026-01-31

Сделано сегодня
- Добавлен BaseDropdownInput с фильтрацией, выбором только из списка и навигацией стрелками.
- Подключен BaseDropdownInput вместо валютного инпута в ReportRowCreateForm.
- BaseListBox поддерживает активный пункт (для стрелочной навигации).

Что проверить завтра
- Поведение автоскролла к активному пункту при навигации стрелками.
- Нужно ли закрытие по Tab/Shift+Tab без потери выбора.
- Визуальные нюансы: ширина дропдауна, отступы лейбла, hover/active состояния.

Файлы
- src/shared/ui/BaseDropdownInput.vue
- src/shared/ui/BaseListBox.vue
- src/widgets/report-builder/ui/ReportRowCreateForm.vue

Заметка на 2026-02-02
1-Чекнуть формирование избранной валюты в сторе и листбоксе - usedCurrencyCodes в мета сторе чекнуть
2-Перенести конвертацию в метод стора
3-Избранные валюты в сторе брать из списка всех валют + проверять валюты на актуальность и удалять из списка неактуальные валюты до 2020 года. 
4-Проверка валюты на актуальность по дате - предупреждение выдавать
5- Формирование даты в инпуте, проверить форматирование



что то с типами посмотреть - src/app/AppHeader.vue:38 -           @update:model-value="(value) => localeStore.setLocale(value as typeof localeStore.currentLocale)"

Заметка на 2026-02-05
1-Сабмит формы и запись в хранилище